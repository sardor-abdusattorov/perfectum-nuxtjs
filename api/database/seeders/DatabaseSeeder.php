<?php

declare(strict_types=1);

namespace Database\Seeders;

use Illuminate\Database\Seeder;

/**
 * The seeders that carried the old site over — its news, tariffs, services,
 * devices, offices and the rest — were a one-time move and left with the
 * launch, along with the dumps they read. A stand is raised from a database
 * dump now; anything worth seeding again gets its own class here.
 */
class DatabaseSeeder extends Seeder
{
    public function run(): void
    {
        //
    }
}

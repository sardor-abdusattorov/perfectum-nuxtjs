<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Trusted Proxies
    |--------------------------------------------------------------------------
    |
    | Кому верить, когда в запросе приходит X-Forwarded-For. От этого зависит,
    | что вернёт $request->ip(): по нему считаются все лимиты и он же пишется
    | в заявку, так что доверять здесь кому попало нельзя — заголовок подделает
    | любой, и лимит станет украшением.
    |
    | nginx отдаёт /api прямо в PHP-FPM, поэтому обычный посетитель приходит
    | своим адресом и заголовок игнорируется. Оставить localhost нужно ради
    | серверного рендеринга: Nuxt ходит в API с той же машины и пересылает
    | адрес читателя, иначе весь сайт считался бы одним посетителем.
    |
    | Если перед nginx стоит балансировщик — впишите сюда его адрес через
    | запятую, иначе посетители сложатся в один счётчик. «*» верит всем и
    | годится только там, где до приложения снаружи не достучаться.
    |
    | Значение читается из конфига, а не из env() напрямую: деплой выполняет
    | config:cache, а после него .env не загружается, и env() в bootstrap
    | молча вернул бы значение по умолчанию.
    |
    */

    'proxies' => env('TRUSTED_PROXIES', '127.0.0.1'),

];
